import java.util.Scanner;

public class Electric {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int total = 0;
        for (int i = 1; i <= 12; i++) {
            System.out.print("Enter the unit for month " + i + ": ");
            int unit = sc.nextInt();
            total += unit;
        }
        double average = total / 12.0; 

        if (average > 500) {
            System.out.println("High Consumption!!");
        } else {
            System.out.println("Less Consumption!");
        }

        
    }
}
